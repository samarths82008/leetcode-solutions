class Solution:
    def sumOddLengthSubarrays(self, arr: List[int]) -> int:
        s = []
        for n in range(1,len(arr)+1,2):
            for i in range(len(arr)+1-n):
                s.append(arr[i:i+n])
    
        return(s(sum(x) for x in s))
