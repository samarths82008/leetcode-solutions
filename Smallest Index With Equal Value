class Solution(object):
    def smallestEqual(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        i = 0 
        while i < len(nums):
            if i % 10 == nums[i]:
                return i
            else:
                i += 1
        return -1
