class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        k=[]
        for i in range(len(nums)):
            if i+n<=len(nums)-1:
                k.append(nums[i])
                k.append(nums[i+n])
        return k
